# Simple Dockerfile for Go microservice
FROM golang:1.24-alpine AS builder


WORKDIR /app

COPY go.mod go.sum ./
RUN go mod download

COPY . .

RUN go build -o reservations-service ./

EXPOSE 8080

CMD ["/app/reservations-service"]
